export enum DrawerState {
    Hidden = 'hidden',
    Peek = 'peek',
    Card = 'card',
    Most = 'most',
    Full = 'full',
};

export enum HeaderState {
    Hidden = 'hidden',
    Visible = 'visible',
};

export enum ItemState {
    None = 'none',
    Preview = 'preview',
    Full = 'full',
};